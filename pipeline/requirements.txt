# pipeline/ deps — pinned for reproducible ingestion.
# Upgrade deliberately; audit tests in pipeline/tests/ gate the DAG.

# --- fetch / normalize ---
httpx==0.27.2
beautifulsoup4==4.12.3
lxml==5.3.0
cloudscraper==1.2.71

# --- Supabase / Postgres ---
supabase==2.9.1
psycopg[binary]==3.2.3
pgvector==0.3.6
python-dotenv==1.0.1

# --- embeddings ---
sentence-transformers==3.2.1
numpy==2.1.3

# --- dev / audit ---
pytest==8.3.3
pytest-asyncio==0.24.0
ruff==0.7.4
